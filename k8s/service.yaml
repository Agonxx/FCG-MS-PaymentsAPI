apiVersion: v1
kind: Service
metadata:
  name: paymentsapi
  namespace: fiapcloudgames
spec:
  selector:
    app: paymentsapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
